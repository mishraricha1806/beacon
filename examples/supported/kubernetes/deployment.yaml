apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments-api
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: api
          image: payments:latest
          securityContext:
            privileged: true
