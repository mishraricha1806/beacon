rule_id: kafka.consumer.security.sasl_plain
title: Kafka consumer SASL/PLAIN mechanism
description: Detects Kafka consumers configured with SASL/PLAIN.
severity_default: MEDIUM
category: operational_safety
author: beacon.rules
recommendation: Prefer SCRAM, OAuth, mTLS, or an approved platform mechanism; require SASL_SSL if PLAIN is unavoidable.
version: "1.0"
tags: [kafka, consumer, security, sasl]
