rule_id: kafka.runtime.decision.monitor_capacity
version: '1.0'
title: 'Kafka runtime decision: monitor capacity'
description: Beacon does not see urgent disk risk from current runtime signals.
severity_default: LOW
category: runtime_stability
author: beacon.runtime
recommendation: Continue monitoring disk growth, producer rate, message size, consumer
  lag, and under-replicated partitions.
