rule_id: kafka.runtime.network_saturation.high
version: '1.0'
title: Kafka network saturation high
description: Kafka broker network I/O utilization is high.
severity_default: HIGH
category: runtime_stability
author: beacon.runtime
recommendation: Review broker network throughput, cross-AZ replication, client traffic,
  and compression settings.
