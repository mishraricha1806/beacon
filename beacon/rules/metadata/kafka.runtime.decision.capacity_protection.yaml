rule_id: kafka.runtime.decision.capacity_protection
version: '1.0'
title: 'Kafka runtime decision: capacity protection'
description: Beacon recommends immediate capacity protection action.
severity_default: CRITICAL
category: runtime_stability
author: beacon.runtime
recommendation: Create headroom first, then investigate producer volume, message size,
  and consumer lag.
