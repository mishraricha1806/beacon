rule_id: kafka.runtime.rebalance_storm
version: '1.0'
title: Kafka rebalance storm
description: Consumer group rebalance frequency is high.
severity_default: HIGH
category: runtime_stability
author: beacon.runtime
recommendation: Inspect deployment churn, heartbeat/session timeouts, max.poll settings,
  and consumer crashes.
