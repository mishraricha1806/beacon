rule_id: kafka.topic.segment_bytes.large
title: Kafka segment bytes large
description: Detects Kafka topics with large log segment size.
severity_default: MEDIUM
category: storage_sustainability
author: beacon.rules
recommendation: Tune segment size so retention cleanup, compaction, and recovery do not lag behind workload needs.
version: "1.0"
tags: [kafka, segment, cleanup]
