rule_id: kafka.cluster.under_min_isr_partitions
version: '1.0'
title: Kafka cluster partitions below safe ISR
description: One or more Kafka partitions are below a safe ISR threshold.
severity_default: CRITICAL
category: resiliency
author: beacon.runtime
recommendation: Restore ISR health before increasing producer pressure or rolling
  more brokers.
